export default class Link {
  /**
   * @param text
   * @param title
   * @param messageUrl
   * @param picUrl
   */
  constructor (text, title, messageUrl, picUrl) {
    this.text = text
    this.title = title
    this.messageUrl = messageUrl
    this.picUrl = picUrl
  }
}
